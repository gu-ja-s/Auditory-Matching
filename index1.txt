<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>소리 버튼 페이지</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            background-color: #f0f0f0;
        }

        .container {
            text-align: center;
        }

        .button-row {
            display: flex;
            justify-content: center;
            margin-top: 20px;
        }

        .sound-button {
            width: 100px;  /* 버튼 너비 */
            height: 100px; /* 버튼 높이 */
            margin: 0 10px;
            border: 2px solid #007BFF; /* 버튼 테두리 */
            background-color: #007BFF; /* 버튼 배경 색상 */
            border-radius: 10px; /* 둥근 모서리 */
            cursor: pointer;
            transition: background-color 0.3s, transform 0.2s; /* 부드러운 효과 */
        }

        .sound-button:hover {
            background-color: #0056b3; /* 마우스 오버시 색상 변화 */
            transform: scale(1.05); /* 약간 커지는 효과 */
        }

        .navigation {
            margin-top: 30px;
        }
    </style>
</head>
<body>
    <div class="container">
        <button id="dogSound1" class="sound-button" title="강아지 소리 1"></button>
        <div class="button-row">
            <button id="dogSound2" class="sound-button" title="강아지 소리 2"></button>
            <button id="waterSound" class="sound-button" title="물방울 소리"></button>
            <button id="trumpetSound" class="sound-button" title="트럼펫 소리"></button>
        </div>
        <div class="navigation">
            <button id="prevPage">◀</button>
            <button id="nextPage">▶</button>
        </div>
    </div>
    <script>
        document.getElementById('dogSound1').addEventListener('click', () => {
            const audio = new Audio('소리/홈페이지만들기/dog1.mp3'); // 강아지 소리 1 파일 경로
            audio.play();
        });

        document.getElementById('dogSound2').addEventListener('click', () => {
            const audio = new Audio('소리/홈페이지만들기/dog2.mp3'); // 강아지 소리 2 파일 경로
            audio.play();
        });

        document.getElementById('waterSound').addEventListener('click', () => {
            const audio = new Audio('소리/홈페이지만들기/water.mp3'); // 물방울 소리 파일 경로
            audio.play();
        });

        document.getElementById('trumpetSound').addEventListener('click', () => {
            const audio = new Audio('소리/홈페이지만들기/trumpet.mp3'); // 트럼펫 소리 파일 경로
            audio.play();
        });

        // 페이지 이동을 위한 이벤트 리스너
        document.getElementById('prevPage').addEventListener('click', () => {
            alert('이전 페이지로 이동합니다.'); // 실제 이동 로직 추가 가능
        });

        document.getElementById('nextPage').addEventListener('click', () => {
            alert('다음 페이지로 이동합니다.'); // 실제 이동 로직 추가 가능
        });
    </script>
</body>
</html>
